type Query {
    bookByName(name: String): [Book]
}

type Book {
    id: ID
    name: String
    image: String
    description: String
    author: String
    price: Float
    inventory: Int
    isbn: String
}