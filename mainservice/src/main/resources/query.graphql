query bookDetails($name:String) {
    bookByName(name: $name) {
        id
        name
        author
        image
        price
        author
        description
        inventory
        isbn
    }
}